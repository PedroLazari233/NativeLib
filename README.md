# NativeLib
Minimal example of C++ and C# interop using P/Invoke. Includes a native C++ library and a C# console application that calls a HelloWorld function and outputs the result. Useful as a starting point for integrating native libraries into .NET applications.
